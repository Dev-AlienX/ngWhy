import { InlineCode } from './inline-code';

describe('InlineCode', () => {
  it('should create an instance', () => {
    const directive = new InlineCode();
    expect(directive).toBeTruthy();
  });
});
